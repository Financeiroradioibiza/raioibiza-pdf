{
  "name": "radioibiza-pdf",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install && npx puppeteer browsers install chrome"
  },
  "dependencies": {
    "express": "^4.18.0",
    "puppeteer": "^21.0.0"
  }
}
